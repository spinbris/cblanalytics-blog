# Shot list - Post A images (all from SYNTHETIC runs only)

Hygiene gate for every capture: no client names in window titles,
tabs, file paths, taskbar or recent-files lists; crop tight to the
app; synthetic labels visible in frame (that is a feature).

IMG-1 dashboard PM overview
  Live Vercel app, synthetic 4-period arc loaded. Full page,
  browser chrome cropped.

IMG-2 Excel dashboard sheet
  Synthetic wind (or Tropic Ridge) workbook, dashboard tab.
  Include the validation/reconciliation status panel if it fits;
  otherwise capture it as a second crop and we choose.

IMG-3 tornado / criticality chart
  Do not screenshot: have Builder regenerate the engine's own PNG
  from a synthetic run (crisper, re-generatable).

IMG-4 cost + schedule S-curve overlay
  Same: engine-generated PNG, synthetic run.

IMG-5 dashboard stochastic Gantt
  Synthetic periods. This is the money shot; capture at width
  >=1600px.

IMG-6 dashboard what-changed / delta view
  Pick the period pair with the clearest visible movement.

Drop files into this post's images/ folder named img1.png ... 
img6.png and I will place and caption them.
